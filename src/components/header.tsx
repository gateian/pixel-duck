import { Typography } from '@mui/material';

const Header = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        PixelDuck
      </Typography>
    </div>
  );
};

export default Header;
